export type DieType = {
    id: string;
    isLocked: boolean;
    value: number;
};

export type ScoreType = number | undefined;

export type ScoreValueType = string | number;

export type DictType = { [key: string]: number };
